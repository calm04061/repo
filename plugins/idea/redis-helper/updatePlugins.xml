<?xml version="1.0" encoding="UTF-8"?>
<!--
The <plugins> element (required) contains the description of the plugins
available at this repository.
-->
<plugins>
    <!--
    Each <plugin> element (required) describes one plugin in the repository.
    Attributes:
     - "id" (required) - used by JetBrains IDEs to uniquely identify
       a plugin. Must match <id> in the plugin.xml file.
     - "url" (required) - URL to download the plugin JAR/ZIP file.
       Must be HTTPS.
     - "version" (required) - version of this plugin. Must match <version>
       in the plugin.xml file.
    -->
    <plugin
            id="com.calm.redis.helper"
            url="https://calm04061.github.io/repo/plugins/idea/redis-helper/redis-helper-1.0.0-SNAPSHOT.zip"
            version="0.0.1">
        <idea-version since-build="213" until-build="223.*"/>
    </plugin>
    <plugin
            id="com.calm.redis.helper"
            url="https://calm04061.github.io/repo/plugins/idea/redis-helper/redis-helper-0.0.2-SNAPSHOT.zip"
            version="0.0.2">
        <idea-version since-build="213" until-build="223.*"/>
    </plugin>
    <plugin
            id="com.calm.redis.helper"
            url="https://calm04061.github.io/repo/plugins/idea/redis-helper/redis-helper-0.0.3.1.zip"
            version="0.0.3.1">
        <idea-version since-build="213" until-build="223.*"/>
    </plugin>
    <!-- And so on for other plugins... -->
</plugins>